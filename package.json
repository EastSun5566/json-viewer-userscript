{
  "name": "json-viewer-userscript",
  "version": "0.0.1",
  "description": "🖼️ A userScript to beautify JSON display in browser",
  "main": "index.js",
  "scripts": {
    "lint": "eslint . --fix --ignore-path .gitignore",
    "release": "npx standard-version && git push --follow-tags"
  },
  "keywords": [
    "userscript",
    "json",
    "beautify",
    "viewer"
  ],
  "author": "Michael Wang <michael19920327@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.57.1",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.32.0",
    "simple-git-hooks": "^2.13.1"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "*.js": "pnpm run lint"
  }
}
